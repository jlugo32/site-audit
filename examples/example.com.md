# Website Security Checkup - example.com

**Date:** 2026-09-15 &nbsp; **Score:** 60/100 &nbsp; **Grade:** C

1 critical, 5 warnings. Checked from the outside only - the way an attacker would see it.

## Critical - fix these first

| Check | Result | Why it matters |
|---|---|---|
| Forces HTTPS | plain http serves the full site | The unencrypted version works. On public wifi, anyone nearby can read what visitors send - including login cookies. |

## Warnings - should fix

| Check | Result | Why it matters |
|---|---|---|
| DMARC reporting | no rua= address | Without a report address you never learn who is spoofing you. |
| HSTS header | missing | A visitor's first request can be downgraded to http by an attacker on the same network. |
| Clickjacking protection | no X-Frame-Options / frame-ancestors | Another site can load this one in an invisible frame and hijack clicks. |
| MIME sniffing blocked | missing X-Content-Type-Options | Browsers may execute a file as script even when it is not meant to be. |
| Content-Security-Policy | missing | No allow-list for scripts. Any injected script runs. Common gap, worth fixing on sites that take payments or logins. |

## Passed - already good

| Check | Result | Why it matters |
|---|---|---|
| Domain resolves | 104.20.23.154 | The site has an address on the internet. |
| SPF record | present, strict (-all) | Receivers are told to reject mail from unlisted servers. |
| DMARC policy | p=reject | Spoofed mail is thrown away. Strongest setting. |
| HTTPS available | https answers (200) | Encrypted connection is offered. |
| Certificate valid | 42 days left (SSL Corporation) | Certificate is healthy. |
| Certificate trusted | chain and name verify | Browsers trust this certificate. |
| Software version hidden | cloudflare | Attackers are not told which software or version is running. |
| Sensitive files exposed | none of the common ones | Config, backup and source-control files are not downloadable. |
| Admin login pages reachable | none at common paths | No obvious admin door on the front of the site. |

## Info - for reference

| Check | Result | Why it matters |
|---|---|---|
| Email (MX) configured | null MX (accepts no mail) | The domain explicitly says it never receives email. Spoofing protection below still matters for SENDING. |
| DKIM signing key | only empty/revoked keys (p=) | An empty key tells receivers this domain does not sign mail. Correct for a domain that sends no email; a problem if it does. |
| Referrer-Policy | missing | Minor. Other sites can see the full URL a visitor came from. |
| Open ports | behind Cloudflare - origin server is hidden | Good: attackers cannot reach the server directly either. Ports on this IP belong to Cloudflare's network, not to this site. |

---
_Generated by site-audit 1.0.0. Findings are based on public information only; fixing them needs access to DNS and/or the server._
